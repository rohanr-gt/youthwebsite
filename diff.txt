diff --git a/src/main/java/com/example/demo/MainController.java b/src/main/java/com/example/demo/MainController.java
index e8d23de..ababd00 100644
--- a/src/main/java/com/example/demo/MainController.java
+++ b/src/main/java/com/example/demo/MainController.java
@@ -225,6 +225,34 @@ public class MainController {
         return "about";
     }
 
+    @GetMapping("/faq")
+    public String faq(Model model, HttpSession session, HttpServletRequest request) {
+        validateSessionOnPublicPage(session, request);
+        model.addAttribute("user", getUserFromSession(session));
+        return "faq";
+    }
+
+    @GetMapping("/terms")
+    public String terms(Model model, HttpSession session, HttpServletRequest request) {
+        validateSessionOnPublicPage(session, request);
+        model.addAttribute("user", getUserFromSession(session));
+        return "terms";
+    }
+
+    @GetMapping("/privacy")
+    public String privacy(Model model, HttpSession session, HttpServletRequest request) {
+        validateSessionOnPublicPage(session, request);
+        model.addAttribute("user", getUserFromSession(session));
+        return "privacy";
+    }
+
+    @GetMapping("/organizers")
+    public String organizers(Model model, HttpSession session, HttpServletRequest request) {
+        validateSessionOnPublicPage(session, request);
+        model.addAttribute("user", getUserFromSession(session));
+        return "organizers";
+    }
+
     @GetMapping("/games")
     public String games(Model model, HttpSession session, HttpServletRequest request) {
         validateSessionOnPublicPage(session, request);
@@ -280,6 +308,11 @@ public class MainController {
         return "candy-crush";
     }
 
+    @GetMapping("/play-memory")
+    public String playMemory() {
+        return "memory-game";
+    }
+
     @GetMapping("/play-runner")
     public String playRunner() {
         return "runner";
@@ -490,6 +523,12 @@ public class MainController {
         model.addAttribute("followersCount", followersCount);
         model.addAttribute("totalPeopleCount", allUsers.size());
 
+        // Pending follow requests for current user
+        List<FollowRequest> followRequests = followRequestRepository.findAll().stream()
+                .filter(r -> r.getReceiver().getId().equals(finalUser.getId()))
+                .collect(Collectors.toList());
+        model.addAttribute("followRequests", followRequests);
+
         return "explore";
     }
 
diff --git a/src/main/java/com/example/demo/controller/AuthController.java b/src/main/java/com/example/demo/controller/AuthController.java
index a245111..1afa74b 100644
--- a/src/main/java/com/example/demo/controller/AuthController.java
+++ b/src/main/java/com/example/demo/controller/AuthController.java
@@ -38,6 +38,9 @@ public class AuthController {
         if (userRepository.findByUsername(user.getUsername()) != null) {
             return "redirect:/register?error=duplicate";
         }
+        if (user.getDob() != null && user.getDob().isAfter(java.time.LocalDate.now())) {
+            return "redirect:/register?error=future_dob";
+        }
         userRepository.save(user);
         return "redirect:/home";
     }
diff --git a/src/main/java/com/example/demo/controller/EventController.java b/src/main/java/com/example/demo/controller/EventController.java
index d14b42f..e08ed6d 100644
--- a/src/main/java/com/example/demo/controller/EventController.java
+++ b/src/main/java/com/example/demo/controller/EventController.java
@@ -434,7 +434,7 @@ public class EventController {
         session.setAttribute("regTier", selectedTier);
         session.setAttribute("regSeatIds", selectedSeatIds);
 
-        if ("Paid".equalsIgnoreCase(event.getEntryFeeType())) {
+        if ("Paid".equalsIgnoreCase(event.getEntryFeeType()) || (event.getPrice() != null && !event.getPrice().equalsIgnoreCase("Free"))) {
             User dbUser = userRepository.findById(user.getId()).orElse(user);
             if (dbUser.isHasFreeEntry()) {
                 dbUser.setHasFreeEntry(false);
diff --git a/src/main/java/com/example/demo/controller/ProfileController.java b/src/main/java/com/example/demo/controller/ProfileController.java
index 418641e..14765df 100644
--- a/src/main/java/com/example/demo/controller/ProfileController.java
+++ b/src/main/java/com/example/demo/controller/ProfileController.java
@@ -77,6 +77,7 @@ public class ProfileController {
     @Autowired
     private com.example.demo.repository.EventRegistrationRepository eventRegistrationRepository;
 
+    @Transactional(readOnly = true)
     @GetMapping("/{username}")
     public String showPublicProfile(@PathVariable String username, HttpSession session, Model model) {
         User currentUser = (User) session.getAttribute("user");
@@ -169,6 +170,7 @@ public class ProfileController {
         return showPublicProfile(user.getUsername(), session, model);
     }
 
+    @Transactional
     @PostMapping("/update")
     public String updateProfile(@RequestParam String username,
             @RequestParam String email,
@@ -185,27 +187,44 @@ public class ProfileController {
         }
         User user = (User) sessionUser;
 
-        User dbUser = userRepository.findById(user.getId()).orElse(null);
-        if (dbUser != null) {
-            dbUser.setUsername(username);
-            dbUser.setEmail(email);
-            if (dob != null && !dob.isEmpty()) {
-                dbUser.setDob(java.time.LocalDate.parse(dob));
+        try {
+            User dbUser = userRepository.findById(user.getId()).orElse(null);
+            if (dbUser != null) {
+                dbUser.setUsername(username);
+                dbUser.setEmail(email);
+                if (dob != null && !dob.trim().isEmpty()) {
+                    java.time.LocalDate birthDate = java.time.LocalDate.parse(dob);
+                    if (birthDate.isAfter(java.time.LocalDate.now())) {
+                        System.err.println("Attempted to set future date of birth: " + dob);
+                    } else {
+                        dbUser.setDob(birthDate);
+                    }
+                }
+                if (gender != null)
+                    dbUser.setGender(gender);
+                if (profilePhotoUrl != null)
+                    dbUser.setProfilePhotoUrl(profilePhotoUrl.length() > 255 ? profilePhotoUrl.substring(0, 255) : profilePhotoUrl);
+                if (aboutMe != null)
+                    dbUser.setAboutMe(aboutMe.length() > 1000 ? aboutMe.substring(0, 1000) : aboutMe);
+                if (skills != null)
+                    dbUser.setSkills(skills.length() > 255 ? skills.substring(0, 255) : skills);
+                if (collegeName != null)
+                    dbUser.setCollegeName(collegeName.length() > 255 ? collegeName.substring(0, 255) : collegeName);
+                userRepository.save(dbUser);
+                // Force reload with collections if needed, or just set the basic user
+                session.setAttribute("user", dbUser);
+            }
+            return "redirect:/profile?success";
+        } catch (Exception e) {
+            System.err.println("Error updating profile for user: " + username);
+            e.printStackTrace();
+            String errorMessage = e.getMessage() != null ? e.getMessage() : "Unknown error";
+            try {
+                return "redirect:/profile?error=" + java.net.URLEncoder.encode(errorMessage, "UTF-8");
+            } catch (java.io.UnsupportedEncodingException ex) {
+                return "redirect:/profile?error=encoding_error";
             }
-            if (gender != null)
-                dbUser.setGender(gender);
-            if (profilePhotoUrl != null)
-                dbUser.setProfilePhotoUrl(profilePhotoUrl);
-            if (aboutMe != null)
-                dbUser.setAboutMe(aboutMe);
-            if (skills != null)
-                dbUser.setSkills(skills);
-            if (collegeName != null)
-                dbUser.setCollegeName(collegeName);
-            userRepository.save(dbUser);
-            session.setAttribute("user", dbUser);
         }
-        return "redirect:/profile?success";
     }
 
     @PostMapping("/reset-password")
diff --git a/src/main/resources/application.properties b/src/main/resources/application.properties
index e36437e..0288830 100644
--- a/src/main/resources/application.properties
+++ b/src/main/resources/application.properties
@@ -1,12 +1,13 @@
 spring.application.name=zentrix1
 # Removed redundant exclusion to ensure standard datasource initialization
 
-server.port=8082
+server.port=9005
+spring.devtools.restart.enabled=false
 
 # Database Configuration
 spring.datasource.url=jdbc:mysql://localhost:3306/zentrix1_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
 spring.datasource.username=root
-spring.datasource.password=root
+spring.datasource.password=Root
 
 spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
 
diff --git a/src/main/resources/static/css/style.css b/src/main/resources/static/css/style.css
index 034e20d..0f19133 100644
--- a/src/main/resources/static/css/style.css
+++ b/src/main/resources/static/css/style.css
@@ -1568,6 +1568,10 @@ input[type="file"].form-input::file-selector-button:hover {
     box-shadow: 0 18px 60px rgba(55, 65, 81, 0.25);
 }
 
+.modal.open {
+    display: block;
+}
+
 .modal-header {
     text-align: center;
     margin-bottom: 20px;
@@ -1593,6 +1597,12 @@ input[type="file"].form-input::file-selector-button:hover {
     backdrop-filter: blur(5px);
 }
 
+.overlay.open {
+    display: flex;
+    opacity: 1;
+    pointer-events: all;
+}
+
 /* About Page */
 .about-hero {
     height: 40vh;
diff --git a/src/main/resources/templates/achievements.html b/src/main/resources/templates/achievements.html
index 5dbc018..d55580b 100644
--- a/src/main/resources/templates/achievements.html
+++ b/src/main/resources/templates/achievements.html
@@ -701,6 +701,7 @@
 
         </main>
     </div>
+    <div th:replace="~{fragments/modals :: createPostModal}"></div>
 </body>
 
 </html>
\ No newline at end of file
diff --git a/src/main/resources/templates/candy-crush.html b/src/main/resources/templates/candy-crush.html
index f091c0f..d959067 100644
--- a/src/main/resources/templates/candy-crush.html
+++ b/src/main/resources/templates/candy-crush.html
@@ -138,7 +138,10 @@
         }
 
         .cell.hint {
+            border: 3px solid gold !important;
+            transform: scale(1.1);
             animation: pulse 0.6s ease-in-out infinite alternate;
+            z-index: 5;
         }
 
         @keyframes pulse {
@@ -285,7 +288,7 @@
         /*<![CDATA[*/
         const COLS = 8, ROWS = 8;
         const CANDIES = ['≡ƒì¡', '≡ƒì¼', '≡ƒì½', '≡ƒìí', '≡ƒºü', '≡ƒì⌐', '≡ƒìª'];
-        let board = [], selected = null, score = 0, best = 0, movesLeft = 30;
+        let board = [], selected = null, score = 0, best = parseInt(localStorage.getItem('candyBest')) || 0, movesLeft = 30;
         let busy = false, hintCells = [];
 
         const boardEl = document.getElementById('board');
@@ -336,7 +339,7 @@
         function cellEl(i) { return boardEl.children[i]; }
 
         function onCellClick(i) {
-            if (busy) return;
+            if (busy || movesLeft <= 0) return;
             clearHint();
 
             if (selected === null) {
@@ -486,7 +489,7 @@
 
         function checkGameOver() {
             if (movesLeft <= 0) {
-                if (score > best) { best = score; bestEl.textContent = best; }
+                if (score > best) { best = score; bestEl.textContent = best; localStorage.setItem('candyBest', best); }
                 showMessage('Game Over! ≡ƒÿó\nScore: ' + score);
                 return;
             }
diff --git a/src/main/resources/templates/car-game.html b/src/main/resources/templates/car-game.html
index a0fb483..b035998 100644
--- a/src/main/resources/templates/car-game.html
+++ b/src/main/resources/templates/car-game.html
@@ -177,6 +177,14 @@
     <!-- REACT UI -->
     <script type="text/babel">
         /*<![CDATA[*/
+        window.onerror = function(msg, url, line, col, error) {
+            const errDiv = document.createElement('div');
+            errDiv.style.cssText = 'position:fixed;top:0;left:0;width:100%;background:rgba(255,0,0,0.9);color:white;z-index:9999;padding:20px;text-align:center;font-family:sans-serif;';
+            errDiv.innerHTML = '<h2>Game Error</h2><p>' + msg + '</p><button onclick="this.parentElement.remove()" style="padding:10px;margin-top:10px;cursor:pointer;">Dismiss</button>';
+            document.body.appendChild(errDiv);
+            return false;
+        };
+
         const { useState, useEffect } = React;
 
         const GameUI = () => {
diff --git a/src/main/resources/templates/chess.html b/src/main/resources/templates/chess.html
index 9b63730..69710c8 100644
--- a/src/main/resources/templates/chess.html
+++ b/src/main/resources/templates/chess.html
@@ -12,6 +12,15 @@
     <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
     <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
     <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
+    <script>
+        window.onerror = function(msg, url, line, col, error) {
+            const errDiv = document.createElement('div');
+            errDiv.style.cssText = 'position:fixed;top:0;left:0;width:100%;background:rgba(255,0,0,0.9);color:white;z-index:9999;padding:20px;text-align:center;font-family:sans-serif;';
+            errDiv.innerHTML = '<h2>Game Error</h2><p>' + msg + '</p><button onclick="this.parentElement.remove()" style="padding:10px;margin-top:10px;cursor:pointer;">Dismiss</button>';
+            document.body.appendChild(errDiv);
+            return false;
+        };
+    </script>
     <script src="https://cdnjs.cloudflare.com/ajax/libs/sockjs-client/1.6.1/sockjs.min.js"></script>
     <script src="https://cdnjs.cloudflare.com/ajax/libs/stomp.js/2.3.3/stomp.min.js"></script>
 
diff --git a/src/main/resources/templates/dashboard.html b/src/main/resources/templates/dashboard.html
index 4669c32..2c7fef1 100644
--- a/src/main/resources/templates/dashboard.html
+++ b/src/main/resources/templates/dashboard.html
@@ -677,60 +677,52 @@
             </div>
         </div>
     </div>
-    <!-- Create Post Modal -->
-    <div class="modal" id="createPostModal" style="display:none; max-width:520px; z-index:2200;">
-        <div class="modal-header">
-            <h3>Create Post</h3>
-            <i class="fas fa-times close-modal" onclick="closeCreateModal()"></i>
-        </div>
-        <div style="padding:20px;">
-            <form id="createPostForm" th:action="@{/profile/post}" method="POST" enctype="multipart/form-data">
-                <textarea name="content" placeholder="What's on your mind?" required
-                    style="width:100%; min-height:110px; background:var(--card-bg); border:1px solid var(--glass-border); border-radius:12px; padding:12px; color:var(--text-primary); font-size:14px; resize:vertical; outline:none; font-family:inherit; box-sizing:border-box;"></textarea>
-                <div style="display:flex; gap:10px; margin-top:12px; flex-wrap:wrap;">
-                    <input name="hashtags" placeholder="#hashtags"
-                        style="flex:1; min-width:120px; padding:9px 12px; border-radius:10px; background:var(--card-bg); border:1px solid var(--glass-border); color:var(--text-primary); font-size:13px; outline:none;">
-                    <select name="postType"
-                        style="flex:1; min-width:120px; padding:9px 12px; border-radius:10px; background:var(--card-bg); border:1px solid var(--glass-border); color:var(--text-primary); font-size:13px; outline:none;">
-                        <option value="POST">Normal Post</option>
-                        <option value="REEL">Reel (Video)</option>
-                        <option value="THOUGHT">Thought (Testimonial)</option>
-                        <option value="STORY">Story (24h)</option>
-                    </select>
-                    <select name="category"
-                        style="flex:1; min-width:120px; padding:9px 12px; border-radius:10px; background:var(--card-bg); border:1px solid var(--glass-border); color:var(--text-primary); font-size:13px; outline:none;">
-                        <option value="">Category (optional)</option>
-                        <option value="GAMING">Gaming</option>
-                        <option value="FITNESS">Fitness</option>
-                        <option value="LIFESTYLE">Lifestyle</option>
-                        <option value="MUSIC">Music</option>
-                        <option value="ART">Art</option>
-                        <option value="FOOD">Food</option>
-                        <option value="TRAVEL">Travel</option>
-                        <option value="TECH">Tech</option>
-                        <option value="SPORTS">Sports</option>
-                        <option value="OTHER">Other</option>
-                    </select>
-                </div>
-                <div style="margin-top:12px;">
-                    <label
-                        style="display:flex; align-items:center; gap:8px; cursor:pointer; font-size:13px; color:var(--text-secondary); background:var(--card-bg); border:1px dashed var(--glass-border); border-radius:10px; padding:10px 14px;">
-                        <i class="fas fa-image" style="color:var(--accent);"></i>
-                        <span>Add Photo / Video</span>
-                        <input type="file" name="media" accept="image/*,video/*" style="display:none;"
-                            onchange="showFileName(this)">
-                    </label>
-                    <span id="selectedFileName"
-                        style="font-size:12px; color:var(--text-secondary); margin-top:6px; display:block;"></span>
-                </div>
-                <button type="submit"
-                    style="margin-top:16px; width:100%; padding:12px; background:var(--accent); color:white; border:none; border-radius:12px; font-size:15px; font-weight:700; cursor:pointer;">
-                    <i class="fas fa-paper-plane"></i> Post
-                </button>
-            </form>
-        </div>
-    </div>
-    <div class="overlay" id="overlay"></div>
+    <div th:replace="~{fragments/modals :: createPostModal}"></div>
+    <div class="overlay" id="overlay" onclick="closeAllModals()"></div>
+
+    <script>
+        function openM(id) {
+            const el = document.getElementById(id);
+            if (el) {
+                el.classList.add('open');
+                el.style.display = 'block'; 
+                const modal = el.querySelector('.modal');
+                if (modal) modal.style.display = 'block';
+            }
+            const overlay = document.getElementById('overlay');
+            if (overlay) {
+                overlay.classList.add('open');
+                overlay.style.display = 'block';
+            }
+        }
+        function closeM(id) {
+            const el = document.getElementById(id);
+            if (el) {
+                el.classList.remove('open');
+                el.style.display = 'none';
+                const modal = el.querySelector('.modal');
+                if (modal) modal.style.display = 'none';
+            }
+            const overlay = document.getElementById('overlay');
+            if (overlay) {
+                overlay.classList.remove('open');
+                overlay.style.display = 'none';
+            }
+        }
+        function closeAllModals() {
+            document.querySelectorAll('.modal').forEach(m => {
+                m.classList.remove('open');
+                m.style.display = 'none';
+            });
+            const overlay = document.getElementById('overlay');
+            if (overlay) {
+                overlay.classList.remove('open');
+                overlay.style.display = 'none';
+            }
+            // Also close the fragment modal if it's open
+            if (typeof closeCreateModal === 'function') closeCreateModal();
+        }
+    </script>
     <!-- Share Modal -->
     <div class="modal" id="shareModal" style="display:none; z-index:2100;">
         <div class="modal-header">
@@ -768,26 +760,12 @@
         });
 
         // ΓöÇΓöÇ Create Post Modal ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
-        function openCreateModal() {
-            document.getElementById('createPostModal').style.display = 'block';
-            document.getElementById('overlay').style.display = 'block';
-        }
         function openThoughtModal() {
-            // Pre-select THOUGHT type in the dropdown
-            const sel = document.querySelector('select[name="postType"]');
-            if (sel) sel.value = 'THOUGHT';
-            // Update placeholder to be more relevant
-            const ta = document.querySelector('textarea[name="content"]');
-            if (ta) ta.placeholder = 'Share your experience with Zentrix... (this will appear on the public home page!)';
             openCreateModal();
-        }
-        function closeCreateModal() {
-            document.getElementById('createPostModal').style.display = 'none';
-            document.getElementById('overlay').style.display = 'none';
-        }
-        function openM(id) {
-            document.getElementById(id).style.display = 'block';
-            document.getElementById('overlay').style.display = 'block';
+            const btn = document.querySelector('.type-btn[data-type="THOUGHT"]');
+            if (btn) selectPostType(btn);
+            const ta = document.querySelector('#postContent');
+            if (ta) ta.placeholder = 'Share your experience with Zentrix... (this will appear on the public home page!)';
         }
         function showFileName(input) {
             const span = document.getElementById('selectedFileName');
@@ -985,8 +963,7 @@
         let allFollowers = [];
         async function openShareModal(postId) {
             currentSharePostId = postId;
-            document.getElementById('shareModal').style.display = 'block';
-            document.getElementById('overlay').style.display = 'block';
+            openM('shareModal');
             document.getElementById('shareSearchInput').value = '';
 
             try {
@@ -1035,8 +1012,7 @@
         }
 
         function closeShareModal() {
-            document.getElementById('shareModal').style.display = 'none';
-            document.getElementById('overlay').style.display = 'none';
+            closeM('shareModal');
         }
         async function shareToUser(userId) {
             const res = await fetch('/api/chat/share-post-to-user', {
@@ -1054,8 +1030,7 @@
         }
 
         function openNotifications() {
-            document.getElementById('notifModal').style.display = 'block';
-            document.getElementById('overlay').style.display = 'block';
+            openM('notifModal');
 
             // Clear badge and mark as read immediately
             const badge = document.getElementById('notif-badge');
diff --git a/src/main/resources/templates/event-registration.html b/src/main/resources/templates/event-registration.html
index c220e86..40b982c 100644
--- a/src/main/resources/templates/event-registration.html
+++ b/src/main/resources/templates/event-registration.html
@@ -1433,7 +1433,7 @@
                         <div class="input-wrap">
                             <i class="fas fa-phone"></i>
                             <input type="tel" name="phone" id="regPhone" placeholder="+91 98765 43210" maxlength="15"
-                                required>
+                                required pattern="[0-9]{10,15}" oninput="this.value = this.value.replace(/[^0-9+]/g, '')">
                         </div>
                     </div>
                 </div>
diff --git a/src/main/resources/templates/events.html b/src/main/resources/templates/events.html
index edb91ee..ea32eef 100644
--- a/src/main/resources/templates/events.html
+++ b/src/main/resources/templates/events.html
@@ -219,12 +219,11 @@
                 <!-- Empty State -->
                 <div th:if="${#lists.isEmpty(events)}" style="text-align:center; padding:80px 20px;">
                     <i class="fas fa-search" style="font-size:48px; color:#D1D5DB; margin-bottom:16px;"></i>
-                    <h3 style="color:#6B7280;">No events found</h3>
+                    <h3 style="color:#6B7280;">No upcoming events. Check back later!</h3>
                     <p style="color:#9CA3AF;">
                         <span th:if="${activeSearch != null and !activeSearch.isEmpty()}">
                             No events match the location &ldquo;<strong th:text="${activeSearch}">query</strong>&rdquo;.
                         </span>
-                        Try a different location, category, or check back later!
                     </p>
                     <a href="/events" style="display:inline-block; margin-top:20px; color:var(--purple); font-weight:700;">
                         <i class="fas fa-times-circle"></i> Reset All Filters
@@ -241,7 +240,8 @@
         // Submit form with selected category pill value
         function setCategory(cat) {
             document.getElementById('categoryInput').value = cat;
-            document.getElementById('eventsFilterForm').submit();
+            const searchVal = document.getElementById('searchInput') ? document.getElementById('searchInput').value : '';
+            window.location.href = '/events?category=' + encodeURIComponent(cat) + (searchVal ? '&search=' + encodeURIComponent(searchVal) : '');
         }
 
         // Show/hide clear button based on input content
@@ -261,5 +261,6 @@
         }
     </script>
 
+    <div th:replace="~{fragments/modals :: createPostModal}"></div>
 </body>
 </html>
\ No newline at end of file
diff --git a/src/main/resources/templates/explore.html b/src/main/resources/templates/explore.html
index c9852d0..0ed40b7 100644
--- a/src/main/resources/templates/explore.html
+++ b/src/main/resources/templates/explore.html
@@ -239,6 +239,64 @@
             .explore-search-panel { flex-direction: column; }
             .explore-search-field { min-width: 100%; }
         }
+
+        /* ΓöÇΓöÇ Follow Requests Section ΓöÇΓöÇ */
+        .follow-requests-section {
+            background: rgba(255, 255, 255, 0.95);
+            border: 1.5px solid var(--accent);
+            border-radius: 20px;
+            padding: 24px;
+            margin-bottom: 30px;
+            box-shadow: 0 12px 40px rgba(248, 68, 100, 0.12);
+        }
+
+        .fr-title {
+            font-size: 18px;
+            font-weight: 800;
+            color: var(--text-primary);
+            margin-bottom: 18px;
+            display: flex;
+            align-items: center;
+            gap: 10px;
+        }
+
+        .fr-grid {
+            display: grid;
+            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
+            gap: 15px;
+        }
+
+        .fr-card {
+            background: #fff;
+            border: 1px solid var(--glass-border);
+            border-radius: 15px;
+            padding: 15px;
+            display: flex;
+            align-items: center;
+            justify-content: space-between;
+            transition: all 0.2s;
+        }
+
+        .fr-card:hover { transform: scale(1.02); box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
+
+        .fr-user-info { display: flex; align-items: center; gap: 12px; text-decoration: none; color: inherit; }
+        .fr-avatar { width: 42px; height: 42px; border-radius: 50%; object-fit: cover; border: 2px solid var(--glass-border); }
+        .fr-username { font-weight: 700; font-size: 14px; }
+        .fr-college { font-size: 11px; color: var(--primary); }
+
+        .fr-actions { display: flex; gap: 8px; }
+        .fr-btn {
+            padding: 8px 14px;
+            border-radius: 10px;
+            font-size: 12px;
+            font-weight: 700;
+            cursor: pointer;
+            border: none;
+            transition: 0.2s;
+        }
+        .fr-btn-accept { background: var(--accent); color: #fff; }
+        .fr-btn-reject { background: #f1f5f9; color: var(--text-secondary); }
+        .fr-btn:hover { opacity: 0.8; }
     </style>
     <script th:src="@{/js/tab-support.js}"></script>
 </head>
@@ -284,6 +342,30 @@
         </aside>
 
         <main class="explore-container">
+        
+        <!-- ΓòÉΓòÉ Pending Follow Requests ΓòÉΓòÉ -->
+        <div th:if="${not #lists.isEmpty(followRequests)}" class="follow-requests-section">
+            <div class="fr-title">
+                <i class="fas fa-user-clock" style="color: var(--accent);"></i>
+                Follow Requests
+                <span class="explore-count-badge" style="background: var(--accent); color: #fff; border: none;" th:text="${#lists.size(followRequests)}">0</span>
+            </div>
+            <div class="fr-grid">
+                <div th:each="fr : ${followRequests}" class="fr-card" th:id="'fr-row-' + ${fr.id}">
+                    <a th:href="@{'/profile/' + ${fr.sender.username}}" class="fr-user-info">
+                        <img th:src="${fr.sender.profilePhotoUrl != null and !fr.sender.profilePhotoUrl.isEmpty() ? fr.sender.profilePhotoUrl : 'https://ui-avatars.com/api/?name=' + fr.sender.username + '&background=f84464&color=fff'}" class="fr-avatar">
+                        <div>
+                            <div class="fr-username" th:text="${fr.sender.username}">Username</div>
+                            <div class="fr-college" th:if="${fr.sender.collegeName != null}" th:text="${fr.sender.collegeName}">College</div>
+                        </div>
+                    </a>
+                    <div class="fr-actions">
+                        <button class="fr-btn fr-btn-accept" th:onclick="'respondToRequest(' + ${fr.id} + ', true)'">Accept</button>
+                        <button class="fr-btn fr-btn-reject" th:onclick="'respondToRequest(' + ${fr.id} + ', false)'">Reject</button>
+                    </div>
+                </div>
+            </div>
+        </div>
         <!-- ΓòÉΓòÉ Search Panel ΓòÉΓòÉ -->
         <div class="explore-search-panel">
             <div class="explore-search-field">
@@ -443,7 +525,31 @@
                 wrapper.innerHTML = original;
             }
         }
+
+        /* ΓöÇΓöÇΓöÇ Accept / Reject Follow Request AJAX ΓöÇΓöÇΓöÇ */
+        async function respondToRequest(requestId, accept) {
+            const row = document.getElementById('fr-row-' + requestId);
+            const endpoint = `/profile/follow-request/${requestId}/${accept ? 'accept' : 'reject'}/ajax`;
+            
+            try {
+                const res = await fetch(endpoint, { method: 'POST' });
+                if (res.ok) {
+                    row.style.opacity = '0';
+                    setTimeout(() => {
+                        row.remove();
+                        // Hide section if empty
+                        const grid = document.querySelector('.fr-grid');
+                        if (grid && grid.children.length === 0) {
+                            document.querySelector('.follow-requests-section').style.display = 'none';
+                        }
+                    }, 300);
+                }
+            } catch (err) {
+                console.error(err);
+            }
+        }
     </script>
 
+    <div th:replace="~{fragments/modals :: createPostModal}"></div>
 </body>
 </html>
\ No newline at end of file
diff --git a/src/main/resources/templates/fragments/template.html b/src/main/resources/templates/fragments/template.html
index db56f79..f67118e 100644
--- a/src/main/resources/templates/fragments/template.html
+++ b/src/main/resources/templates/fragments/template.html
@@ -105,14 +105,14 @@
                 <a href="/about">About Us</a>
                 <a href="/events">Events</a>
                 <a href="/support">Contact</a>
-                <a href="#">Organizers</a>
+                <a href="/organizers">Organizers</a>
             </div>
             <div class="footer-col">
                 <h3>Help</h3>
                 <a href="/support">Contact Us</a>
-                <a href="#">FAQs</a>
-                <a href="#">Terms of Service</a>
-                <a href="#">Privacy Policy</a>
+                <a href="/faq">FAQs</a>
+                <a href="/terms">Terms of Service</a>
+                <a href="/privacy">Privacy Policy</a>
             </div>
         </div>
         <script th:src="@{/js/animations.js}"></script>
diff --git a/src/main/resources/templates/login.html b/src/main/resources/templates/login.html
index 72d7455..58a9225 100644
--- a/src/main/resources/templates/login.html
+++ b/src/main/resources/templates/login.html
@@ -101,6 +101,11 @@
 </head>
 
 <body>
+    <div style="position: absolute; top: 30px; left: 30px; z-index: 10;">
+        <a href="/home" style="display: inline-flex; align-items: center; gap: 8px; color: var(--text-primary); text-decoration: none; font-weight: 700; background: rgba(255,255,255,0.8); padding: 10px 16px; border-radius: 12px; border: 1px solid var(--glass-border); box-shadow: 0 4px 15px rgba(0,0,0,0.05); transition: transform 0.2s;">
+            <i class="fas fa-arrow-left"></i> Back to Website
+        </a>
+    </div>
 
     <div class="login-container">
         <h2>Welcome Back</h2>
@@ -130,7 +135,10 @@
             </div>
 
             <div class="form-group">
-                <label for="password">Password</label>
+                <div style="display:flex; justify-content:space-between; margin-bottom: 0.5rem;">
+                    <label for="password" style="margin-bottom:0;">Password</label>
+                    <a href="/forgot-password" style="font-size: 13px; font-weight: 700; color: var(--primary); text-decoration: none;">Forgot Password?</a>
+                </div>
                 <input type="password" id="password" name="password" required>
             </div>
 
diff --git a/src/main/resources/templates/ludo.html b/src/main/resources/templates/ludo.html
index aab18e2..67707d9 100644
--- a/src/main/resources/templates/ludo.html
+++ b/src/main/resources/templates/ludo.html
@@ -681,8 +681,23 @@
         document.addEventListener('DOMContentLoaded', () => {
             const input = document.getElementById('playerNameInput');
             if (input && !input.value) input.value = userName;
+            
+            const rId = new URLSearchParams(window.location.search).get('room');
+            if (rId) {
+                document.getElementById('roomCodeInput').value = rId;
+                // Auto-join
+                setTimeout(joinRoom, 500);
+            }
         });
 
+        window.onerror = function(msg, url, line, col, error) {
+            const errDiv = document.createElement('div');
+            errDiv.style.cssText = 'position:fixed;top:0;left:0;width:100%;background:rgba(255,0,0,0.9);color:white;z-index:9999;padding:20px;text-align:center;font-family:sans-serif;';
+            errDiv.innerHTML = '<h2>Game Error</h2><p>' + msg + '</p><button onclick="this.parentElement.remove()" style="padding:10px;margin-top:10px;cursor:pointer;">Dismiss</button>';
+            document.body.appendChild(errDiv);
+            return false;
+        };
+
         const BOARD_SIZE = Math.min(520, window.innerWidth * 0.92);
         const C = BOARD_SIZE / 15;
         const canvas = document.getElementById('boardCanvas');
diff --git a/src/main/resources/templates/mario.html b/src/main/resources/templates/mario.html
index 3f75ef7..8a6e3ad 100644
--- a/src/main/resources/templates/mario.html
+++ b/src/main/resources/templates/mario.html
@@ -427,7 +427,8 @@
             <div id="game-over-screen" style="display:none;">
                 <h2>GAME OVER</h2>
                 <div class="go-score">SCORE: <span id="final-score">0</span></div>
-                <button class="play-btn" id="restart-btn">Γû╢ TRY AGAIN</button>
+                <div class="go-score" style="margin-top: 5px; color: #ffdd00;">BEST: <span id="best-score">0</span></div>
+                <button class="play-btn" id="restart-btn" style="margin-top: 10px;">Γû╢ TRY AGAIN</button>
                 <div style="margin-top: 10px;">
                     <a href="/games" class="exit-btn">Exit Game</a>
                 </div>
@@ -497,6 +498,8 @@
         let particles = [];
         let coinAnims = [];
         let frameCount = 0;
+        let jumpPressed = false;
+        let bestScore = parseInt(localStorage.getItem('marioBest')) || 0;
 
         // ΓöÇΓöÇ Mario ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
         const mario = {
@@ -595,7 +598,7 @@
                 { x: 4640, h: 3 }, { x: 5056, h: 2 }, { x: 5312, h: 3 },
                 { x: 5600, h: 2 }, { x: 5760, h: 4 },
             ];
-            pp.forEach(p => pipes.push({ x: p.x, y: GROUND_Y - TILE * p.h, w: TILE * 2, h: TILE * p.h + TILE * .25, headY: GROUND_Y - TILE * p.h - 8 }));
+            pp.forEach(p => pipes.push({ x: p.x, y: GROUND_Y - TILE * p.h - 8, w: TILE * 2, h: TILE * p.h + 8, headY: GROUND_Y - TILE * p.h - 8 }));
 
             // Enemies (Goombas)
             const es = [
@@ -1046,9 +1049,17 @@
                 if (mario.vx > speedLimit) mario.vx = speedLimit;
                 if (mario.vx < -speedLimit) mario.vx = -speedLimit;
 
-                if ((keys['ArrowUp'] || keys['w'] || keys[' ']) && mario.onGround) {
-                    mario.vy = JUMP_FORCE;
-                    mario.onGround = false;
+                if (keys['ArrowUp'] || keys['w'] || keys[' ']) {
+                    if (mario.onGround && !jumpPressed) {
+                        mario.vy = JUMP_FORCE;
+                        mario.onGround = false;
+                    }
+                    jumpPressed = true;
+                } else {
+                    jumpPressed = false;
+                    if (mario.vy < 0) {
+                        mario.vy *= 0.5; // Variable jump height
+                    }
                 }
 
                 mario.vy += GRAVITY;
@@ -1099,6 +1110,11 @@
                     lives--;
                     if (lives <= 0) { 
                         gameState = 'gameover'; 
+                        if (score > bestScore) {
+                            bestScore = score;
+                            localStorage.setItem('marioBest', bestScore);
+                        }
+                        document.getElementById('best-score').textContent = bestScore;
                         document.getElementById('game-over-screen').style.display = 'flex'; 
                         GameRewards.award('Super Mario', 'PLAY'); // Participation reward
                     }
diff --git a/src/main/resources/templates/messages.html b/src/main/resources/templates/messages.html
index 01edf71..4bf27c2 100644
--- a/src/main/resources/templates/messages.html
+++ b/src/main/resources/templates/messages.html
@@ -3890,6 +3890,7 @@
         }
     </script>
 
+    <div th:replace="~{fragments/modals :: createPostModal}"></div>
 </body>
 
 </html>
\ No newline at end of file
diff --git a/src/main/resources/templates/music-leaderboard.html b/src/main/resources/templates/music-leaderboard.html
index b17e3b7..e0d2b2b 100644
--- a/src/main/resources/templates/music-leaderboard.html
+++ b/src/main/resources/templates/music-leaderboard.html
@@ -9,9 +9,9 @@
     <style>
         .card { background: var(--card-bg); border: 1px solid var(--glass-border); border-radius: 18px; padding: 16px; }
         .grid { display:grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 16px; }
-        .row { display:flex; justify-content:space-between; gap: 12px; padding: 10px 0; border-bottom: 1px solid rgba(255,255,255,0.08); }
+        .row { display:flex; justify-content:space-between; gap: 12px; padding: 10px 0; border-bottom: 1px solid var(--glass-border); }
         .row:last-child { border-bottom: none; }
-        .rank { width: 28px; height: 28px; border-radius: 10px; display:flex; align-items:center; justify-content:center; font-weight: 900; background: rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.10); }
+        .rank { width: 28px; height: 28px; border-radius: 10px; display:flex; align-items:center; justify-content:center; font-weight: 900; background: rgba(0,0,0,0.06); border:1px solid var(--glass-border); }
     </style>
 </head>
 <body>
@@ -57,14 +57,14 @@
         <main class="content-main">
             <div class="container" style="margin-top: 0;">
     <h1 style="margin-bottom: 6px;">Music Leaderboard</h1>
-    <div style="color: rgba(255,255,255,0.55); font-size: 13px; margin-bottom: 18px;">
+    <div style="color: var(--text-muted); font-size: 13px; margin-bottom: 18px;">
         Based on rooms created and votes cast (MVP).
     </div>
 
     <div class="grid">
         <div class="card">
             <h3 style="margin:0 0 12px 0;"><i class="fas fa-thumbs-up" style="color:#3b82f6;"></i> Top voters</h3>
-            <div th:if="${topVoters == null or topVoters.isEmpty()}" style="color: rgba(255,255,255,0.6); text-align:center; padding: 10px;">
+            <div th:if="${topVoters == null or topVoters.isEmpty()}" style="color: var(--text-secondary); text-align:center; padding: 10px;">
                 No votes yet.
             </div>
             <div th:each="r,iter : ${topVoters}" class="row">
@@ -81,7 +81,7 @@
 
         <div class="card">
             <h3 style="margin:0 0 12px 0;"><i class="fas fa-microphone" style="color: var(--accent);"></i> Top hosts</h3>
-            <div th:if="${topHosts == null or topHosts.isEmpty()}" style="color: rgba(255,255,255,0.6); text-align:center; padding: 10px;">
+            <div th:if="${topHosts == null or topHosts.isEmpty()}" style="color: var(--text-secondary); text-align:center; padding: 10px;">
                 No rooms yet.
             </div>
             <div th:each="r,iter : ${topHosts}" class="row">
@@ -99,6 +99,7 @@
             </div>
         </main>
     </div>
+    <div th:replace="~{fragments/modals :: createPostModal}"></div>
 </body>
 </html>
 
diff --git a/src/main/resources/templates/music-rooms.html b/src/main/resources/templates/music-rooms.html
index 68080e0..4a91c68 100644
--- a/src/main/resources/templates/music-rooms.html
+++ b/src/main/resources/templates/music-rooms.html
@@ -9,7 +9,7 @@
     <style>
         .card { background: var(--card-bg); border: 1px solid var(--glass-border); border-radius: 18px; padding: 16px; }
         .grid { display:grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 16px; }
-        .pill { font-size: 11px; padding: 3px 10px; border-radius: 999px; border: 1px solid rgba(255,255,255,0.12); color: rgba(255,255,255,0.65); }
+        .pill { font-size: 11px; padding: 3px 10px; border-radius: 999px; border: 1px solid var(--glass-border); color: var(--text-secondary); }
         .warn { background: rgba(245,158,11,0.12); border-color: rgba(245,158,11,0.25); color: #f59e0b; }
     </style>
 </head>
@@ -58,7 +58,7 @@
     <div style="display:flex; justify-content:space-between; gap: 16px; align-items:flex-start; flex-wrap:wrap;">
         <div>
             <h1 style="margin-bottom: 6px;">Music Rooms</h1>
-            <div style="color: rgba(255,255,255,0.55); font-size: 13px;">
+            <div style="color: var(--text-muted); font-size: 13px;">
                 Create a room, share the code, submit songs, and let spectators vote.
             </div>
         </div>
@@ -88,13 +88,13 @@
                 <input class="form-input" name="localTitle" placeholder="Local song title (optional)" style="flex:1; min-width: 220px;">
                 <input class="form-input" name="localArtistName" placeholder="Local artist name (optional)" style="flex:1; min-width: 220px;">
                 <input class="form-input" type="file" name="localFile" accept="audio/*" style="flex:1; min-width: 220px;">
-                <label style="display:flex; gap:8px; align-items:center; font-size: 12px; color: rgba(255,255,255,0.7); flex:1; min-width: 220px;">
+                <label style="display:flex; gap:8px; align-items:center; font-size: 12px; color: var(--text-secondary); flex:1; min-width: 220px;">
                     <input type="checkbox" name="acceptLocalTerms">
                     I own rights to stream this local audio in this room.
                 </label>
                 <button class="nav-btn" type="submit" style="padding: 10px 14px;"><i class="fas fa-wand-magic-sparkles"></i> Generate code</button>
             </form>
-            <div style="margin-top:10px; font-size:12px; color: rgba(255,255,255,0.55);">
+            <div style="margin-top:10px; font-size:12px; color: var(--text-muted);">
                 Pick either an approved track or upload a local audio file (room-only). Selected music will be auto-submitted into the room.
             </div>
         </div>
@@ -110,13 +110,13 @@
                 <input class="form-input" name="localTitle" placeholder="Local song title (optional)" style="flex:1; min-width: 220px;">
                 <input class="form-input" name="localArtistName" placeholder="Local artist name (optional)" style="flex:1; min-width: 220px;">
                 <input class="form-input" type="file" name="localFile" accept="audio/*" style="flex:1; min-width: 220px;">
-                <label style="display:flex; gap:8px; align-items:center; font-size: 12px; color: rgba(255,255,255,0.7); flex:1; min-width: 220px;">
+                <label style="display:flex; gap:8px; align-items:center; font-size: 12px; color: var(--text-secondary); flex:1; min-width: 220px;">
                     <input type="checkbox" name="acceptLocalTerms">
                     I own rights to stream this local audio in this room.
                 </label>
                 <button class="nav-btn" type="submit" style="padding: 10px 14px; background: rgba(59,130,246,0.14); border:1px solid rgba(59,130,246,0.35);"><i class="fas fa-door-open"></i> Join</button>
             </form>
-            <div style="margin-top:10px; font-size:12px; color: rgba(255,255,255,0.55);">
+            <div style="margin-top:10px; font-size:12px; color: var(--text-muted);">
                 Join as spectator (no track) or join as competitor (select a track).
             </div>
         </div>
@@ -124,6 +124,7 @@
             </div>
         </main>
     </div>
+    <div th:replace="~{fragments/modals :: createPostModal}"></div>
 </body>
 </html>
 
diff --git a/src/main/resources/templates/music.html b/src/main/resources/templates/music.html
index bbc911a..4f36f2a 100644
--- a/src/main/resources/templates/music.html
+++ b/src/main/resources/templates/music.html
@@ -3,11 +3,12 @@
 <head th:replace="~{fragments/template :: head('Music | Zentrix')}">
     <meta charset="UTF-8">
 </head>
+<body class="music-page">
     <style>
         .music-grid { display:grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 18px; }
-        .music-card { background: #FFFFFF; border: 1px solid var(--glass-border); border-radius: 18px; padding: 16px; box-shadow: 0 10px 30px rgba(0,0,0,0.03); }
-        .pill { font-size: 11px; padding: 3px 10px; border-radius: 999px; border: 1px solid rgba(255,255,255,0.12); color: rgba(255,255,255,0.65); }
-        .success { background: rgba(16,185,129,0.12); border-color: rgba(16,185,129,0.25); color: #34d399; }
+        .music-card { background: rgba(255,255,255,0.6); border: 1px solid var(--glass-border); border-radius: 18px; padding: 16px; box-shadow: 0 10px 30px rgba(0,0,0,0.03); }
+        .pill { font-size: 11px; padding: 3px 10px; border-radius: 999px; border: 1px solid var(--glass-border); color: var(--text-secondary); }
+        .success { background: rgba(16,185,129,0.12); border-color: rgba(16,185,129,0.25); color: #10b981; }
         .warn { background: rgba(245,158,11,0.12); border-color: rgba(245,158,11,0.25); color: #f59e0b; }
 
         .toast {
@@ -17,7 +18,7 @@
             z-index: 9999;
             background: rgba(16,185,129,0.14);
             border: 1px solid rgba(16,185,129,0.35);
-            color: #34d399;
+            color: #10b981;
             padding: 14px 16px;
             border-radius: 14px;
             box-shadow: 0 14px 40px rgba(0,0,0,0.45);
@@ -30,10 +31,9 @@
             margin-top: 4px;
             font-size: 12px;
             font-weight: 600;
-            color: rgba(255,255,255,0.6);
+            color: var(--text-muted);
         }
     </style>
-</head>
     <!-- Navbar -->
     <nav class="dashboard-navbar">
         <a class="dashboard-logo" href="/home">Zentrix</a>
@@ -141,7 +141,7 @@
                     </div>
                 </div>
                 <form th:action="@{'/music/' + ${t.id} + '/like'}" method="post">
-                    <button class="nav-btn" type="submit" style="padding: 8px 12px; background: rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.12);">
+                    <button class="nav-btn" type="submit" style="padding: 8px 12px; background: rgba(0,0,0,0.03); border:1px solid var(--glass-border);">
                         <i class="fas fa-heart" style="color:#f84464;"></i>
                         <span th:text="${likeCounts[t.id]}">0</span>
                     </button>
@@ -153,7 +153,7 @@
                 <source th:src="@{/music/stream/{id}(id=${t.id},auth=${auth})}" th:type="${t.contentType}">
             </audio>
 
-            <div style="margin-top: 10px; color: rgba(255,255,255,0.5); font-size: 12px;">
+            <div style="margin-top: 10px; color: var(--text-muted); font-size: 12px;">
                 Daily quest: listen 10 minutes ΓåÆ earn +3 coins.
             </div>
         </div>
@@ -219,6 +219,7 @@
         audio.addEventListener('ended', stop);
     });
 </script>
+    <div th:replace="~{fragments/modals :: createPostModal}"></div>
 </body>
 </html>
 
diff --git a/src/main/resources/templates/profile.html b/src/main/resources/templates/profile.html
index 657ddaf..a70b4d6 100644
--- a/src/main/resources/templates/profile.html
+++ b/src/main/resources/templates/profile.html
@@ -9,6 +9,17 @@
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
     <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&display=swap"
         rel="stylesheet">
+    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&display=swap"
+        rel="stylesheet">
+    <script>
+        window.onerror = function(msg, url, line, col, error) {
+            const errDiv = document.createElement('div');
+            errDiv.style.cssText = 'position:fixed;top:0;left:0;width:100%;background:rgba(255,0,0,0.9);color:white;z-index:9999;padding:20px;text-align:center;font-family:sans-serif;';
+            errDiv.innerHTML = '<h2>Page Error</h2><p>' + msg + '</p><button onclick="this.parentElement.remove()" style="padding:10px;margin-top:10px;cursor:pointer;">Dismiss</button>';
+            document.body.appendChild(errDiv);
+            return false;
+        };
+    </script>
     <style>
         /* Additional profile-specific styles that aren't in style.css */
         .follow-list-container {
@@ -19,6 +30,7 @@
             overflow: hidden;
             border: 1px solid var(--glass-border);
             margin-bottom: 40px;
+            box-shadow: var(--shadow-premium);
         }
 
         .follow-item {
@@ -52,6 +64,11 @@
             gap: 52px;
             align-items: flex-start;
             margin-bottom: 40px;
+            background: var(--card-bg);
+            padding: 30px;
+            border-radius: 24px;
+            border: 1px solid var(--glass-border);
+            box-shadow: var(--shadow-premium);
         }
 
         .avatar-wrap {
@@ -110,6 +127,7 @@
         .profile-username {
             font-size: 24px;
             font-weight: 600;
+            color: var(--text-primary);
         }
 
         .btn-edit {
@@ -147,6 +165,7 @@
         .stat-num {
             font-size: 18px;
             font-weight: 700;
+            color: var(--text-primary);
         }
 
         .stat-label {
@@ -515,6 +534,17 @@
     </nav>
 
     <div class="dashboard-container">
+        
+        <!-- Error Alert -->
+        <div th:if="${param.error}" class="container" style="margin-top: 20px;">
+            <div style="background: rgba(239, 68, 68, 0.1); border: 1px solid #ef4444; color: #ef4444; padding: 15px; border-radius: 12px; display: flex; align-items: center; gap: 12px;">
+                <i class="fas fa-exclamation-circle"></i>
+                <div>
+                    <strong style="display: block; font-size: 14px;">Update Failed</strong>
+                    <span style="font-size: 13px;" th:text="${param.error}">Error message here</span>
+                </div>
+            </div>
+        </div>
 
         <!-- Left Sidebar -->
         <aside class="sidebar">
@@ -799,22 +829,25 @@
                             style="width:70px; height:70px; border-radius:50%; object-fit:cover; border:2px solid var(--accent);">
                         <div style="flex:1;">
                             <label style="font-size:12px; color:var(--text-secondary);">Photo URL</label>
-                            <input type="text" name="profilePhotoUrl" th:value="${user.profilePhotoUrl}"
+                            <input type="text" name="profilePhotoUrl" th:value="${user.profilePhotoUrl}" maxlength="255"
                                 class="form-input" style="padding:8px;" oninput="livePhoto(this.value)">
                         </div>
                     </div>
                     <div class="form-group">
                         <label>Username</label>
-                        <input type="text" name="username" th:value="${user.username}" class="form-input" required>
+                        <input type="text" name="username" th:value="${user.username}" class="form-input" maxlength="255" required>
                     </div>
                     <div class="form-group">
                         <label>Email</label>
-                        <input type="email" name="email" th:value="${user.email}" class="form-input" required>
+                        <input type="email" name="email" th:value="${user.email}" class="form-input" maxlength="255" required>
                     </div>
                     <div class="form-group">
                         <label>Date of Birth</label>
-                        <input type="date" name="dob" th:value="${user.dob}" class="form-input">
+                        <input type="date" name="dob" th:value="${user.dob}" class="form-input" id="dobInput">
                     </div>
+                    <script>
+                        document.getElementById('dobInput').max = new Date().toISOString().split("T")[0];
+                    </script>
                     <div class="form-group">
                         <label>Gender</label>
                         <select name="gender" class="form-input">
@@ -825,17 +858,17 @@
                     </div>
                     <div class="form-group">
                         <label>Skills (comma separated)</label>
-                        <input type="text" name="skills" th:value="${user.skills}" class="form-input"
+                        <input type="text" name="skills" th:value="${user.skills}" class="form-input" maxlength="255"
                             placeholder="e.g. Design, Coding">
                     </div>
                     <div class="form-group">
                         <label>College / Institution</label>
-                        <input type="text" name="collegeName" th:value="${user.collegeName}" class="form-input"
+                        <input type="text" name="collegeName" th:value="${user.collegeName}" class="form-input" maxlength="255"
                             placeholder="e.g. IIT Bombay">
                     </div>
                     <div class="form-group">
                         <label>About Me</label>
-                        <textarea name="aboutMe" class="form-input" rows="3" th:text="${user.aboutMe}"></textarea>
+                        <textarea name="aboutMe" class="form-input" rows="3" maxlength="1000" th:text="${user.aboutMe}"></textarea>
                     </div>
                     <button type="submit" class="cta-btn" style="width:100%;">Save Changes</button>
                 </form>
@@ -1160,6 +1193,7 @@
             }
         }
         function closeM(id) { document.getElementById(id).classList.remove('open'); }
+        function openCreateModal() { openM('createPostModal'); }
 
         function switchTab(n, b) {
             document.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'));
@@ -1518,6 +1552,19 @@
             }
         }
 
+        function switchTab(tabId, btn) {
+            // Hide all tab panels
+            document.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'));
+            // Show target tab panel
+            const panel = document.getElementById('tab-' + tabId);
+            if (panel) panel.classList.add('active');
+            
+            // Deactivate all tab buttons
+            document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
+            // Activate clicked button
+            if (btn) btn.classList.add('active');
+        }
+
         // --- STORY VIEWER LOGIC ---
         let currentStoryIndex = 0;
         let currentStories = [];
diff --git a/src/main/resources/templates/register.html b/src/main/resources/templates/register.html
index 259c458..481922c 100644
--- a/src/main/resources/templates/register.html
+++ b/src/main/resources/templates/register.html
@@ -99,6 +99,11 @@
     </style>
 </head>
 <body>
+    <div style="position: absolute; top: 30px; left: 30px; z-index: 10;">
+        <a href="/home" style="display: inline-flex; align-items: center; gap: 8px; color: var(--text-primary); text-decoration: none; font-weight: 700; background: rgba(255,255,255,0.8); padding: 10px 16px; border-radius: 12px; border: 1px solid var(--glass-border); box-shadow: 0 4px 15px rgba(0,0,0,0.05); transition: transform 0.2s;">
+            <i class="fas fa-arrow-left"></i> Back to Website
+        </a>
+    </div>
 
 <div class="register-container">
     <h2>Create Account</h2>
@@ -146,5 +151,11 @@
     </div>
 </div>
 
+<script>
+    document.addEventListener("DOMContentLoaded", function() {
+        var today = new Date().toISOString().split('T')[0];
+        document.getElementById("dob").setAttribute("max", today);
+    });
+</script>
 </body>
 </html>
diff --git a/src/main/resources/templates/rock-paper-scissors.html b/src/main/resources/templates/rock-paper-scissors.html
index 22c3760..c02b0d2 100644
--- a/src/main/resources/templates/rock-paper-scissors.html
+++ b/src/main/resources/templates/rock-paper-scissors.html
@@ -11,6 +11,15 @@
     <meta name="context-path" th:content="@{/}">
     <script src="https://cdnjs.cloudflare.com/ajax/libs/stomp.js/2.3.3/stomp.min.js"></script>
     <script th:src="@{/js/game-rewards.js}"></script>
+    <script>
+        window.onerror = function(msg, url, line, col, error) {
+            const errDiv = document.createElement('div');
+            errDiv.style.cssText = 'position:fixed;top:0;left:0;width:100%;background:rgba(255,0,0,0.9);color:white;z-index:9999;padding:20px;text-align:center;font-family:sans-serif;';
+            errDiv.innerHTML = '<h2>Game Error</h2><p>' + msg + '</p><button onclick="this.parentElement.remove()" style="padding:10px;margin-top:10px;cursor:pointer;">Dismiss</button>';
+            document.body.appendChild(errDiv);
+            return false;
+        };
+    </script>
 
     <style>
         body { background: #0f172a; color: #fff; font-family: 'Inter', sans-serif; }
diff --git a/src/main/resources/templates/shop.html b/src/main/resources/templates/shop.html
index 855a65e..2915888 100644
--- a/src/main/resources/templates/shop.html
+++ b/src/main/resources/templates/shop.html
@@ -296,7 +296,7 @@
                 <p>Get a massive 50% discount on your next paid event registration. One-time use.</p>
                 <div class="price-tag"><i class="fas fa-coins"></i> 100</div>
                 <form th:action="@{/shop/buy-discount}" method="POST">
-                    <button type="submit" class="buy-btn" th:disabled="${user.coins < 100 || user.hasDiscount}">
+                    <button type="submit" class="buy-btn" th:disabled="${user.hasDiscount}">
                         Buy Discount
                     </button>
                 </form>
@@ -310,7 +310,7 @@
                 <p>Get featured the top of user searches and listings for 3 full days. Gain more followers!</p>
                 <div class="price-tag"><i class="fas fa-coins"></i> 200</div>
                 <form th:action="@{/shop/buy-boost}" method="POST">
-                    <button type="submit" class="buy-btn" th:disabled="${user.coins < 200}">
+                    <button type="submit" class="buy-btn">
                         Boost Profile
                     </button>
                 </form>
@@ -324,7 +324,7 @@
                 <p>Register for ANY paid event for absolutely free. Premium and VIP tiers included!</p>
                 <div class="price-tag"><i class="fas fa-coins"></i> 300</div>
                 <form th:action="@{/shop/buy-free-entry}" method="POST">
-                    <button type="submit" class="buy-btn" th:disabled="${user.coins < 300 || user.hasFreeEntry}">
+                    <button type="submit" class="buy-btn" th:disabled="${user.hasFreeEntry}">
                         Buy Ticket
                     </button>
                 </form>
@@ -338,7 +338,7 @@
                 <p>Unlock a permanent golden 'Crown' badge next to your name and unlock exclusive colors.</p>
                 <div class="price-tag"><i class="fas fa-coins"></i> 500</div>
                 <form th:action="@{/shop/buy-badge}" method="POST">
-                    <button type="submit" class="buy-btn" th:disabled="${user.coins < 500 || user.premium}">
+                    <button type="submit" class="buy-btn" th:disabled="${user.premium}">
                         Unlock Badge
                     </button>
                 </form>
diff --git a/src/main/resources/templates/snake-and-ladder.html b/src/main/resources/templates/snake-and-ladder.html
index 53a88bc..9ddc869 100644
--- a/src/main/resources/templates/snake-and-ladder.html
+++ b/src/main/resources/templates/snake-and-ladder.html
@@ -13,6 +13,15 @@
     <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
     <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
     <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
+    <script>
+        window.onerror = function(msg, url, line, col, error) {
+            const errDiv = document.createElement('div');
+            errDiv.style.cssText = 'position:fixed;top:0;left:0;width:100%;background:rgba(255,0,0,0.9);color:white;z-index:9999;padding:20px;text-align:center;font-family:sans-serif;';
+            errDiv.innerHTML = '<h2>Game Error</h2><p>' + msg + '</p><button onclick="this.parentElement.remove()" style="padding:10px;margin-top:10px;cursor:pointer;">Dismiss</button>';
+            document.body.appendChild(errDiv);
+            return false;
+        };
+    </script>
 
     <!-- SockJS + STOMP -->
     <script src="https://cdnjs.cloudflare.com/ajax/libs/sockjs-client/1.6.1/sockjs.min.js"></script>
diff --git a/src/main/resources/templates/uno.html b/src/main/resources/templates/uno.html
index 9c75cdf..7520a66 100644
--- a/src/main/resources/templates/uno.html
+++ b/src/main/resources/templates/uno.html
@@ -12,6 +12,15 @@
     <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
     <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
     <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
+    <script>
+        window.onerror = function(msg, url, line, col, error) {
+            const errDiv = document.createElement('div');
+            errDiv.style.cssText = 'position:fixed;top:0;left:0;width:100%;background:rgba(255,0,0,0.9);color:white;z-index:9999;padding:20px;text-align:center;font-family:sans-serif;';
+            errDiv.innerHTML = '<h2>Game Error</h2><p>' + msg + '</p><button onclick="this.parentElement.remove()" style="padding:10px;margin-top:10px;cursor:pointer;">Dismiss</button>';
+            document.body.appendChild(errDiv);
+            return false;
+        };
+    </script>
 
     <!-- SockJS + STOMP (WebSocket) -->
     <script src="https://cdnjs.cloudflare.com/ajax/libs/sockjs-client/1.6.1/sockjs.min.js"></script>
